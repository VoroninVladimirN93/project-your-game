export enum CLIENT_ROUTES {
    HOME = '/',
    REGISTRATION = '/signup',
    AUTHORIZATION = '/signin',
    LOGOUT = '/logout',
    TASKS = '/tasks',
    NOTFOUND = '*',
};

export enum N {
    A,
    B,
    C
}