export enum CLIENT_ROUTES {
    HOME = "/",
    REGISTRATION = "/signup",
    AUTHORIZATION = "/signin",
    LOGOUT = "/logout",
    NOTFOUND = "*",
    GAME = "/game",
    PROFILE = '/profile'
}

export enum N {
    A,
    B,
    C,
}
