export enum CLIENT_ROUTES {
    HOME = '/',
    PROFILE = '/profile',
    REGISTRATION = '/signup',
    AUTHORIZATION = '/signin',
    LOGOUT = '/logout',
    TASKS = '/tasks',
    NOTFOUND = '*',
    GAME ='/game'
};

export enum N {
    A,
    B,
    C
}